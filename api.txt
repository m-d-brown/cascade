# The exported API, generated by `go generate ./internal/api`.
# A change here is a change to what users can call. Review it as one.

package work
	func Config(fingerprint string) Option
	func Critical() Option
	func Do[T any](ctx *Context, name string, fn func(ctx *Context) (T, error), opts ...Option) (T, error)
	func Doc(doc string) Option
	func Fatal(err error) error
	func Go[T any](ctx *Context, name string, fn func(ctx *Context) (T, error), opts ...Option) *Future[T]
	func LastRecord[T any](ctx *Context, name string, opts ...Option) (value T, meta RecordMeta, ok bool)
	func NewRunner(root func(ctx *Context) (string, error), opts Options) *Runner
	func Secret() Option
	func Skip(format string, a ...any) error
	func Tag(name string) Option
	func Timeout(d time.Duration) Option
	type Context method (*Context) Deadline() (time.Time, bool)
	type Context method (*Context) Done() <-chan struct{}
	type Context method (*Context) Err() error
	type Context method (*Context) LogPath() string
	type Context method (*Context) LogWriter() io.WriteCloser
	type Context method (*Context) Logf(format string, a ...any)
	type Context method (*Context) Monitor(interval time.Duration, probe func(context.Context) (string, error)) (stop func())
	type Context method (*Context) Name() string
	type Context method (*Context) Path() string
	type Context method (*Context) Started() time.Time
	type Context method (*Context) StatusWriter() io.WriteCloser
	type Context method (*Context) Statusf(format string, a ...any)
	type Context method (*Context) Summarize(format string, a ...any)
	type Context method (*Context) Value(key any) any
	type Context method (*Context) Warnf(format string, a ...any)
	type Context struct
	type Future[T any] struct
	type Future[T] method (*Future[T]) Get() (T, error)
	type Option func(*taskConfig)
	type Options field Concurrency int
	type Options field Continue string
	type Options field Input string
	type Options field Logs *history.LogDir
	type Options field Name string
	type Options field Observer history.Observer
	type Options field Plan bool
	type Options field RunID string
	type Options field StopOnError bool
	type Options field Store history.Store
	type Options struct
	type RecordMeta field Finished time.Time
	type RecordMeta field HasValue bool
	type RecordMeta field Started time.Time
	type RecordMeta field Status history.Status
	type RecordMeta field Summary string
	type RecordMeta method (RecordMeta) Age() time.Duration
	type RecordMeta struct
	type Runner method (*Runner) Run(ctx context.Context) (*history.Result, error)
	type Runner struct

package history
	const Canceled
	const Failed
	const Pending Status
	const Resumed
	const RunFinished
	const RunLog
	const RunStarted EventKind
	const Running
	const Skipped
	const Succeeded
	const TaskFinished
	const TaskLog
	const TaskStarted
	const TaskStatus
	func Dot(r Run, opts DotOptions) string
	func Flamegraph(r Run) string
	func LogEvents(w io.Writer) Observer
	func NewFileStore(path string) Store
	func NewLogDir(base string) (*LogDir, error)
	func NewMemoryStore() Store
	func Observers(all ...Observer) Observer
	type DotOptions field Cluster bool
	type DotOptions field LeftToRight bool
	type DotOptions field Title string
	type DotOptions struct
	type Duration method (*Duration) UnmarshalText(b []byte) error
	type Duration method (Duration) MarshalText() ([]byte, error)
	type Duration time.Duration
	type Event field Doc string
	type Event field Kind EventKind
	type Event field Level slog.Level
	type Event field LogPath string
	type Event field Message string
	type Event field Name string
	type Event field Outcome *Result
	type Event field Parent string
	type Event field Path string
	type Event field Result *TaskResult
	type Event field Run string
	type Event field Status Status
	type Event field Tag string
	type Event field Time time.Time
	type Event struct
	type EventKind method (EventKind) MarshalText() ([]byte, error)
	type EventKind method (EventKind) String() string
	type EventKind uint8
	type Journal field Runs []Run
	type Journal field Updated time.Time
	type Journal field Version int
	type Journal method (*Journal) LastSuccessful(path, fingerprint string) (Record, bool)
	type Journal method (*Journal) MostRecent(path string) (Record, string, bool)
	type Journal method (*Journal) Paths() []string
	type Journal method (*Journal) Recent(n int) []Run
	type Journal method (*Journal) Run(id string) (Run, bool)
	type Journal struct
	type LogDir field Dir string
	type LogDir method (*LogDir) Close() error
	type LogDir method (*LogDir) Combined() string
	type LogDir method (*LogDir) Events() io.Writer
	type LogDir method (*LogDir) Run() string
	type LogDir method (*LogDir) WriteCombined(callPath string, level slog.Level, msg string, when time.Time)
	type LogDir struct
	type Observer interface
	type Observer interface-method Handle(Event)
	type Record field Doc string
	type Record field Duration Duration
	type Record field Error string
	type Record field Fingerprint string
	type Record field Finished time.Time
	type Record field HasValue bool
	type Record field Partial bool
	type Record field Path string
	type Record field Secret bool
	type Record field Started time.Time
	type Record field Status Status
	type Record field Summary string
	type Record field Tag string
	type Record field Value any
	type Record struct
	type Result field AbortReason string
	type Result field Aborted bool
	type Result field Counts map[Status]int
	type Result field Duration time.Duration
	type Result field Finished time.Time
	type Result field ID string
	type Result field Order []string
	type Result field Started time.Time
	type Result field Tasks map[string]*TaskResult
	type Result method (*Result) Err() error
	type Result method (*Result) ExitCode() int
	type Result method (*Result) Failed() []*TaskResult
	type Result method (*Result) SortedResults() []*TaskResult
	type Result struct
	type Run field Finished time.Time
	type Run field ID string
	type Run field Input string
	type Run field Logs string
	type Run field Plan bool
	type Run field Started time.Time
	type Run field Status Status
	type Run field Summary string
	type Run field Tasks map[string]Record
	type Run method (Run) Counts() map[Status]int
	type Run method (Run) Done() bool
	type Run method (Run) Restorable(path, fingerprint string) (Record, bool)
	type Run struct
	type Status method (*Status) UnmarshalText(b []byte) error
	type Status method (Status) MarshalText() ([]byte, error)
	type Status method (Status) OK() bool
	type Status method (Status) String() string
	type Status method (Status) Symbol() string
	type Status method (Status) Terminal() bool
	type Status uint8
	type Store interface
	type Store interface-method Load(ctx context.Context) (*Journal, error)
	type Store interface-method Save(ctx context.Context, run Run) error
	type TaskResult field Doc string
	type TaskResult field Duration time.Duration
	type TaskResult field Err error
	type TaskResult field Finished time.Time
	type TaskResult field LogPath string
	type TaskResult field Name string
	type TaskResult field Parent string
	type TaskResult field Path string
	type TaskResult field Started time.Time
	type TaskResult field Status Status
	type TaskResult field Summary string
	type TaskResult field Tag string
	type TaskResult struct

package world
	const AbortRun
	const ApproveAll
	const ApproveOnce Decision
	const ApprovePermanent
	const DryRunOnce
	const SkipOnce
	func Change(call *work.Context, w World, what string, do func() error) error
	func Confirm(w World, p Prompter) World
	func DryRun() World
	func Perform[T any](call *work.Context, w World, e Effect[T]) (T, error)
	func PrompterFromContext(ctx context.Context) Prompter
	func Real() World
	func WithPrompter(ctx context.Context, p Prompter) context.Context
	type Decision int
	type Effect[T any] field Do func() (T, error)
	type Effect[T any] field Instead T
	type Effect[T any] field What string
	type Effect[T any] struct
	type Prompter interface
	type Prompter interface-method PromptApproval(ctx context.Context, req Request) (Decision, error)
	type Request field Target string
	type Request field Task string
	type Request struct
	type World interface
	type World interface-method Perform(call *work.Context, e Effect[any]) (any, error)

package cli
	func Execute(app App) int
	func Main(app App)
	func UserStatePath(name string) string
	type App field DefaultLogDir string
	type App field DefaultStatePath string
	type App field Describe func() string
	type App field ExitWhenDone bool
	type App field Flags func(fs *pflag.FlagSet)
	type App field Flow func(ctx *work.Context) (string, error)
	type App field Long string
	type App field Name string
	type App field Short string
	type App field Version string
	type App struct

package units
	func NewWatcher() *Watcher
	func Run(ctx *work.Context, w world.World, c Cmd) (ExecResult, error)
	func Secret(ctx *work.Context, w world.World, source SecretSource, name, ref string) (string, error)
	func Secrets(ctx *work.Context, w world.World, source SecretSource, name string, refs map[string]string) (map[string]string, error)
	type Cmd field AllowExit []int
	type Cmd field Args []string
	type Cmd field Dir string
	type Cmd field Env []string
	type Cmd field Instead []string
	type Cmd field Path string
	type Cmd field Quiet bool
	type Cmd field Shell string
	type Cmd field Stdin io.Reader
	type Cmd field Watch *Watcher
	type Cmd method (Cmd) String() string
	type Cmd struct
	type ExecResult field DryRun bool
	type ExecResult field Duration time.Duration
	type ExecResult field ExitCode int
	type ExecResult field Lines []string
	type ExecResult method (ExecResult) Find(substr string) (string, bool)
	type ExecResult method (ExecResult) Output() string
	type ExecResult method (ExecResult) Tail(n int) []string
	type ExecResult struct
	type OnePassword field Bin string
	type OnePassword method (OnePassword) ID() string
	type OnePassword method (OnePassword) Read(ctx *work.Context, w world.World, refs []string) (map[string]string, error)
	type OnePassword struct
	type SecretSource interface
	type SecretSource interface-method ID() string
	type SecretSource interface-method Read(ctx *work.Context, w world.World, refs []string) (map[string]string, error)
	type Watcher method (*Watcher) Lines() []string
	type Watcher method (*Watcher) TailLine(n int) string
	type Watcher struct

package ui
	func IsTerminal(f *os.File) bool
	func NewLive(out io.Writer, in io.Reader, cancel func(), exitWhenDone bool) *Live
	func NewPlain(out io.Writer, in io.Reader, verbose bool) *Plain
	func Summary(w io.Writer, res *history.Result)
	type Live method (*Live) Handle(e history.Event)
	type Live method (*Live) PromptApproval(ctx context.Context, req world.Request) (world.Decision, error)
	type Live method (*Live) Start()
	type Live method (*Live) Stop()
	type Live method (*Live) Wait()
	type Live struct
	type Plain method (*Plain) Handle(e history.Event)
	type Plain method (*Plain) PromptApproval(ctx context.Context, req world.Request) (world.Decision, error)
	type Plain struct

package interpreter
	func Load(path string) (*Plan, error)
	func Parse(data []byte) (*Plan, error)
	func ReadProgress(logDir string) (*RunProgress, error)
	type Action field AllowExit []int
	type Action field Description string
	type Action field Dir string
	type Action field Env map[string]string
	type Action field Every string
	type Action field Needs []string
	type Action field Optional bool
	type Action field Produces []string
	type Action field Progress string
	type Action field ProgressEvery string
	type Action field Run string
	type Action field Sources []string
	type Action field Timeout string
	type Action field Unless string
	type Action struct
	type ActionStatus field Detail string
	type ActionStatus field Finished bool
	type ActionStatus field Name string
	type ActionStatus field Started time.Time
	type ActionStatus field Status history.Status
	type ActionStatus field Updated time.Time
	type ActionStatus method (ActionStatus) Elapsed() time.Duration
	type ActionStatus struct
	type Plan method (*Plan) Actions() map[string]Action
	type Plan method (*Plan) Name() string
	type Plan method (*Plan) Order() []string
	type Plan method (*Plan) Run(ctx *work.Context, w world.World) (string, error)
	type Plan method (*Plan) Warnings() []string
	type Plan struct
	type RunProgress field Actions []ActionStatus
	type RunProgress field Finished bool
	type RunProgress field Run string
	type RunProgress field Started time.Time
	type RunProgress struct
